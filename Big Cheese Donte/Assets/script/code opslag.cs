using System.Collections;

/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class codeopslag : MonoBehaviour
{
    //playerHealth
    public float Health;
    public float MaxHealth;
    public GameObject HealthBar;
    public Slider Slider;
    public TextMeshProUGUI healthText;

//player Damage
        Health -= Random.Range(minDmg, maxDmg);

        Health -= amount;

start { 
        Health = MaxHealth;
        Slider.value = CalculateHealth();
}


update { Slider.value = CalculateHealth();

        EnemySlider.value = CalculateEnemyHealth();

        if (Health <= 0 || EnemyHealth <= 0)
        {


            //hier moet wat gebeuren als de enemy of de player 0 is


        }


        healthText.text = Health.ToString() + " / " + MaxHealth.ToString();}

 float CalculateHealth()
    {//als de max health 100 is en health 10 veranderd de healthbar
        return Health / MaxHealth;
    }

} */
